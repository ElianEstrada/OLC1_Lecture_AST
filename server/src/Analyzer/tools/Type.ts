export enum type {
    INT     = 'int',
    STRING  = 'string',
    BOOLEAN = 'boolean'
}

export enum arithmeticOperator {
    PLUS    = '+',
    MINUS   = '-',
    MULT    = '*',
    DIV     = '/'
}

export enum relationalOperator {
    EQ      = '==',
    NEQ     = '!=',
    LT      = '<',
    LTE     = '<=',
    GT      = '>',
    GTE     = '>='
}